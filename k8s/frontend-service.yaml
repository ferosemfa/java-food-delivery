apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: food-delivery
  labels:
    app: food-delivery
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: food-delivery
    component: frontend
