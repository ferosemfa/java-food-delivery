apiVersion: v1
kind: Service
metadata:
  name: cassandra-service
  namespace: food-delivery
  labels:
    app: food-delivery
    component: cassandra
spec:
  type: ClusterIP
  ports:
    - port: 9042
      targetPort: 9042
      protocol: TCP
      name: cql
  selector:
    app: food-delivery
    component: cassandra
